{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build ipykeep extension",
      "type": "shell",
      "command": "npm run build -- --sourcemap",
      "options": { "cwd": "${workspaceFolder}/editors/vscode" },
      "group": "build",
      "problemMatcher": []
    }
  ]
}
